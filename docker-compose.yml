services:
  postgres:
    image: postgres:16-alpine
    ports:
      - '5432:5432'
    environment:
      POSTGRES_DB: blindpass
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: blindpass
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
