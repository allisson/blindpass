VITE_MIN_PASSWORD_LENGTH=12
# Optional. API server URL for the Vite dev proxy (default: http://localhost:3000)
# VITE_API_URL=http://localhost:3000
# Comma-separated hostnames allowed to reach the Vite dev server (e.g. my.tunnel.io,192.168.1.10)
VITE_ALLOWED_HOSTS=
